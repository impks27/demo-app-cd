name: CD Repo PR Checks

on:
  pull_request:

jobs:
  validate-helm:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Helm
      uses: azure/setup-helm@v3

    - name: Lint Helm Chart
      run: helm lint ./chart

